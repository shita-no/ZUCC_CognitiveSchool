<view class = "bc_cavas">
    <view class = "bc" wx:for = "{{numbers}}" wx:for-item = "row" >
      <view id = "{{item}}" wx:for = "{{row}}" class = "bc_ bc_{{item}}" bindtap = "clickBlock">
        <view class = "number">{{item}}</view>
      </view>
    </view>
  </view>